#+title: Tasks

* Completed
+ [X] Notification Tray
  + [X] Format Notification as seen in actual notifications in expanded view (left click)

+ [X] Theme> settings
  + [X] Remove Amber Since its a worse version of Gruvbox...Replace it with an Everforest theme
  + [X] Rename Purple to Pyrple

+ [X] Mpris module
  + [X] While hovering over Mpris module in bar. Scroll with (alt + wheel) to switch players
    + [X] Instead of ctrl do Alt
    + [X] Also make binding for Alt + play to hide/show mpris module
    + [X] Alt + Click the module to toggle between compact/expanded
  + [X] Defaults should be:
    + [X] Hide on idle: false
    + [X] Enable Mpris: true
    + [X] Compact: true
  + [X] Hide keybind in hyprland - toggle visibility on and off
  + [X] Does not persist visibility state across sessions with prefs look into it
    (persistence verified & showMpris defaults to true now)
  + [X] Use different color for mpd - pastel pyprle

+ [X] Wallpaper Rofi
  + [X] Blinking on hover eg. the dark/light toggle favorites blink breaking the UI
  + [X] Currently picked wallpaper should persist across sessions...it seems to reset with every quickshell reload/restart

+ [X] Git
  + [X] Write Instructions on how to use it and its functionality to git-use.org
  + [X] check performance...seems rather intensive for what i want it to do.
  + [X] Use on filechanged where possibel

+ [X] Bluetooth Module
  + [X] Should Persist across sessions eg. if off stay of unless i explicitly turn it on again.
  + [X] Remove the on/off text and just have the switch
  + [X] Check functionality to be intact
  + [X] Implement scan refresh etc...normal bt user expected functionality
    NOTE: quickshell's BT API is reactive-only, so the popup runs a bounded
    bluetoothctl scan (`timeout 8 bluetoothctl scan on`) via a header refresh
    button; discovered devices surface reactively


* Pending
+ [X] Wallpaper Rofi
  + [X] Header UI is broken...fix and modernize view...current implementation is tacky with alot of repetitions and bad design
  + [X] A wallpaper can be both light or dark & also favorite
  + [X] In stead of {all,light,dark, unknown} only have opt-in light & dark toggles where I can click to filter list and unlick to show all wallpapers. Assigned wallpapers should have the light/dark badge visible always like the favorites
  + [X] Move the dark/light buttons/icons to same place as the favorites but only show them on hover on the favorites icon itself not on hover the wallpaper. unless already assigned
+ [X] Settings
  + [X] Hyprland
    + [X] Dual tone borders not working. (parse fix + live verify; needs shell restart to take effect)
    + [X] Reset should use my default hardcoded file values to reset
  + [X] Move ipc handlers from help and make their own dedicated section in settings root
  + [X] IPC handler logic is broken
    + [X] Inspect logic and only have those that make sense to be toggled on/off in settings. eg activate linux
  + [X] Workspaces
    + [X] On disable -> visibility of...currently the space is not reclaimed (width now collapses to 0)
+ [X] Notifications
  + [X] When notification has interactive buttons eg. settings on chrome notifications...their is no margins and the buttons break the look. Compensate for this scenario
+ [X] Now playing
  The Delete button should close the player not just remove it from the list of players. If not possible remove the functionality
  + [X] Expanded View
    + [X] Restore previous design
      + [X] Song title+artist should be in the bottom left
      + [X] On click to show the progress and track buttons the layout should be structured as
        {song art/title}
        {Progress bar}
        The progress should be visible below the song art/title still at the bottom left.
      + [X] The progress bar should adhere to the bottom border instead of float.
      + [X] + should turn to -
  + [X] Compact view
    + [X] The progress bar should adhere to the bottom border instead of float at the center
      Adjust track buttons to match
    + [X] Show playerchooser when players>1 in this view as well
  + [X] Binding changes
    + [X] Git module
      + [X] Do alt+left click to show performance metrics instead of middle click
+ [X] Implement Sticky notes functionality in desktop -- with free notes format and todo
  (free-note and todo modes per note; persist kind/items; Settings adds both kinds)
+ [X] Git needs to be simpler...its very expensive
  + [X] Make settings page for it to adjust its parameters
  (Settings → Git: poll interval + global untracked-scan toggle; defaults persisted)


* TODO LATER
+ [ ] Possible to store sudo access for hdd so i dont have to manually enter it on every hdd? Use userspace tools if not -- what options are available?
