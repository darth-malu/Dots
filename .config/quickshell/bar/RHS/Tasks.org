* Pending
+ [ ] Settings
  + [ ] Bar
    + [ ] Bar Width is broken -- look into it...I just want to be able to increase/decrease its width and also add reset option to restore current settings
    + [ ] Do drop down design for the color theme
+ [ ] Desktop
  + [ ] Sticky Notes
    + [ ] Make this a widget like the clock that I need to interact with to add sticky notes..like kde/microsoft win 7 sticky notes
    + [ ] Remove the setting from settings
      

* New Pending:
 + .
* TODO LATER
+ [ ] Possible to store sudo access for hdd so i dont have to manually enter it on every hdd? Use userspace tools if not -- what options are available?
  + researched: udisksctl exists (/run/current-system/sw/bin/udisksctl) and udisks2 is dbus-activated on NixOS (add services.udisks2.enable = true) — lets you mount/unmount without sudo via D-Bus; automount on boot via fstab noauto,nofail,x-systemd.automount; the only NOPASSWD sudo path is a scoped security.sudo.extraRules entry for /run/wrappers/bin/mount/umount
