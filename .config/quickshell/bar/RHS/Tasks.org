* Pending
+ [X] Settings
  + [X] Bar
    + [X] Bar Width is broken -- FIXED: margins were computed from the window's own width (self-referential → oscillation). Now derived from the screen width so a fixed-width slab genuinely centers and holds. Reset button in the stepper restores the default (0 = full screen).
    + [X] Do drop down design for the color theme -- dropdown with 6 schemes already in place; also fixed the `settingsWindow is not defined` ReferenceError that broke the click-outside close.
  + [X] Desktop
    + [X] Sticky Notes -- REMOVED (2026-09-11): the whole sticky-note feature is gone. Deleted `notBar/sticky/` (StickyNotes.qml, DesktopAddNote.qml), `services/NotesState.qml`, `bar/StickyNotesWidget.qml` and `notes.json`; dropped `import qs.notBar.sticky` + the `StickyNotes {}`/`DesktopAddNote {}` mounts from shell.qml, the `notes` IpcHandler target, and `MiscState.showStickyPopup`. (History: it was a win7/kde-style desktop widget with a floating "+" launcher, note/todo windows, and event-driven persistence — see the long FIXED entry below.)
+ [X] + for new sticky notes does not work. Expected: launch a small sticky notes window like win7 -- FIXED at last (3 layers of blocker): (1) StickyNote.qml referenced the bare `notesModel` id (undefined outside NotesState.qml) so `row()`/`indexOf()` threw — now only uses `NotesState.model`. (2) quickshell 0.3.1 scene `Repeater` rejects non-Item delegates ("Delegate must be of Item type") and `Variants` won't feed `modelData` to FILE-composed delegates (only inline ones, and only as `required property var modelData`; ListModels are rejected entirely with "Non list data"). (3) NotesState seeded from `JsonAdapter` on `Component.onCompleted` while FileView loads async → empty model; and the old file format `{"notes":[...]}` wasn't parsed as an array. RESULT: `StickyNotes.qml` is now a `Variants { model: NotesState.variantNotes; PanelWindow {...} }` with the whole note UI INLINE; `variantsNotes` is a snapshot array rebuilt on structural ops only (add/remove/clear/kind/color/visible/todo-add/remove/toggle) so text/pos/size edits don't churn windows; `notes.json` is read/written directly via `FileView` (`blockLoading: true`, `text()`/`setText()`, plain array format, legacy object format accepted). Persisted notes render, "+" spawns a focused note live, auto-focus is win7-style (only for empty fresh notes). Verified: 2 saved notes render; live addNote created a 3rd window on the fly; clean journal. -- FEATURE SINCE REMOVED (see Desktop > Sticky Notes)
+ [X] Bar
  + [X] GIT
    + [X] Slow...unsynced notification and action happening -- refresh now re-queues (400ms debounce) when busy instead of silently dropping the poke, so the deferred toast + probe always land promptly.
    + [X] Adding a git repo does not work -- `_expand()` treated brace patterns literally. Added `_braceExpand()` so `~/Projects/{Shibuya,Org}` adds every path in the set (skipping duplicates).
  + [X] Empty workspace
    + [X] with icons -> empty workspace badge now uses the same fixed 18px container / formatting as icon workspaces (only the icon area is empty)
    + [X] with number -> resting workspaces are now plain text (no border/bg); only the active pill keeps the border + bg, urgent keeps its red flash
  + workspaces
    + [X] empty workspace number lingering after switching -- workspace pills/badges stay visible by design (switch target must not pop in); empty badge redesigned to match occupied formatting
+ [X] Mpris
  + [X] ALT + Right Click now toggles expanded/compact view (was alt+left); plain right click still = next track
+ [X] Mpris module event map (strict) -- now exactly: left click = pause, right click = next, alt+left = toggle compact/expanded, alt+click ring glyph ("play") = hide-when-idle visibility on/off, alt+wheel = cycle players. Removed middle-click popup, forward/back raise, plain-wheel volume, player-name click-cycling and album-art click popup.
+ [X] Possible to store sudo access for hdd so i dont have to manually enter it on every hdd? Use userspace tools if not -- DONE: fstab already has `users,nofail` entries (UUID-based) for Hyogo/Mutsu/Yuri. `NasState.mount()` now resolves the device from fstab and drives it purely via `udisksctl mount` (no sudo); unmount stays on `udisksctl unmount`. systemd unit is only the fallback if the device can't be resolved. udisksctl/udiskie both present.
+ [X] Look into why my polkit agent is not working -- root cause: hyprpolkitagent is ALSO running and registered with polkitd first, so the quickshell agent never receives requests. Remove hyprpolkitagent from the NixOS config to let `notBar/polkit/PolkitPrompt.qml` take over. Firmware: prompt now grabs keyboard focus reliably (retry loop until the field is visible).
  + [X] Improve its UI-UX in congruence with the rest of the theme -- trimmed to the same card/field language as quicksettings; focus is handed straight to the password field on challenge start.
+ [X] wallpaper rofi
  + [X] Color picker for the borders -- ring swab + palette already wired (PickerState.wallpaperBorder); verified in picker-prefs.json
  + [X] Light/dark icons move to bottom-right corner on hover, not the whole tile -- already implemented (toneCluster)
  + [X] Favorite icon border+bg removed, yellow on check kept -- already implemented (favCluster)
  + [X] Favorite star: only always visible when the wallpaper is favorited; otherwise it surfaces only on hover over its corner region, not the whole tile -- favCluster now mirrors the toneCluster pattern (container always hittable, `favReveal` MouseArea over the 22x22 corner turns on `revealDelayed`; the glyph `favBtn` is `visible: fav || revealDelayed`) so unfavorited stars no longer pop on generic tile hover.
+ [X] Quotes slideshow desktop widget -- new `services/QuotesState.qml` (Singleton: ~21 attributed factoids from Ken Thompson, Ritchie, Kernighan, Torvalds, Hopper, Turing, Dijkstra, Knuth, Stroustrup, Karlton, Hoare, Abelson, Hamming, Kay, Jobs, Carmack + pacing timer, 9s/quote) and `notBar/background/DesktopQuotes.qml` (Variants-per-screen PanelWindow, Bottom layer, `quickshell-quotes`, empty click mask, bottom-center text + attribution, soft crossfade via held-object swap on `onIndexChanged`). Mounted in shell.qml; gated on `WallpaperService.enabled`. Verified: surface renders, picker open/close untouched.
+ [X] Quotes desktop widget restyle (2026-09-11) -- italics removed. New look: thin 26px accent rule above a DemiBold (weight 63, letterSpacing 0.4) quote body in Quicksand, attribution as accent `\u2014` dash + uppercase author (letterSpacing 2, 10px). Crossfade/held-object swap untouched.
+ [X] `.refreshWorkspace()` scoped workspace refresh (2026-09-11) -- `WorkspaceService.refreshWorkspace(name)` refresh a SINGLE workspace instead of the shared clock: each workspace name keeps its own monotonic revision (`_wsRevs` + `wsRevision(name)`); the name is validated against the live `Hyprland.workspaces` model and falls back to the global `refresh()` if it doesn't resolve (so a mis-scoped event can never stall a consumer). `workspacev2`/`focusedmonv2` events (the only list-events that carry the workspace NAME) route through it; every other event keeps the shared clock. `WorkspaceIcons` per-block icon pass now runs off `wsRevision(ws.name)` (new `wsIconRev` binding) instead of the global revision, so switching workspaces no longer recomputes every workspace's icon set. Verified with live `set_workspace` switches via the HyprlandService socket: active-workspace flips, zero journal errors, bar renders.
+ [X] Delete glyph follows the favorite-hover pattern (2026-09-11) -- `tileDelete` in WallpaperPicker.qml no longer reveals on whole-tile hover (`cellMa.containsMouse` dropped). It now mirrors favCluster: the 22x22 bottom-left corner is always hittable via a `deleteReveal` MouseArea (enter → `currentIndex = index` + `revealDelayed = true` + 300ms `delTimer`, exit → restart timer, click → arm confirm); the `\uf1f8` glyph is `visible: tileDelete.revealed` only. Arm-click still flips the full-tile ✓/✗ scrim.
+ [X] Quotes widget overflow + revamp (2026-09-11) -- root cause of the cut-off: `quoteWrap` had no height (left/right/bottom anchors only) so the old top-anchored ColumnLayout started near the screen bottom and spilled off the bottom edge. It's now anchored `bottom: parent.bottom` and grows upward, with an extra 48px floor (`bottomMargin: 48`). Text revamp: heavy DemiBold + black-outlined glyphs are gone; the quote now sits on a soft translucent glass chip (`rgba(0.02,0.03,0.05,0.55)`, 14px radius, 1px white-alpha border) in normal-weight Quicksand 15 (lineHeight 1.4, letterSpacing 0.25), with the author uppercase tracked (letterSpacing 1.8) flanked by two 12x1 accent rules.
+ [X] Delete glyph hover fix (2026-09-11) -- root cause of "doesn't show on hover": the revealed `delBtn` Rectangle was declared AFTER the `deleteReveal` MouseArea, so on reveal it sat on top and stole the mouse → `onExited` fired → 300ms `delTimer` hid it again (blink/hide loop). Fix: glyph rendered BELOW, single `deleteReveal` MouseArea is now the topmost child (stable `containsMouse`, click still arms the ✓/✗ scrim).
+ [X] Notes: `.refreshWorkspace()` per-workspace refresh (2026-09-11) -- `WorkspaceService.refreshWorkspace(name)` + `wsRevision(name)` (validated against live workspaces, falls back to global `refresh()`); `workspacev2`/`focusedmonv2` route through it; `WorkspaceIcons` per-block `wsIconRev` binding re-runs only that workspace's icon pass.
+ [X] Quotes desktop settings (2026-09-11) -- new persisted keys via `Prefs.qml` JsonAdapter + `prefs.json`: `quotesEnabled` (default true), `quotesShowBg` (default false → no glass chip), `quotesQuoteFont` / `quotesAuthorFont` (default "Quicksand"). `WallpaperService.quotesEnabled` gates the surface; `QuotesState.showBg/quoteFont/authorFont` drive DesktopQuotes. Settings > Desktop now has two Cards separated by a hairline: **Desktop** (clock, bar text tone) and **Quotes** (on/off, background, funny extras, hyprland quotes, quote font, author font, quote rotation interval). `PollRow` at 2s–60s persists via new `quotesIntervalMs` (default 9s). Verified: journal clean, round-trips via prefs.json, temp instantiation clean.
+ [X] Quotes deck expansion (2026-09-11) -- `quotes` (programming legends) grows from 21 → 35 with Bill Gates, Arthur C. Clarke, Fowler, Kent Beck, Seymour Cray, Kay x2, St. Exupéry, etc. New groups in `QuotesState.qml`: `hyprQuotes` (10 — genuine Splashes.hpp / vaxry lines: "read the wiki", "war crime", "Woo, animations!", "AUR packages always work...") and `funQuotes` (12 — programmer folklore: "works on my machine", 10 types of people, SQL joins a bar, 99-bugs, sudo rm -rf, booleans-off-by-a-bit...). Deck = `quotes.concat(showHypr?hyprQuotes:[], showFun?funQuotes:[])` cycled by the existing timer; toggling a group calls `next()` so the change shows immediately. Settings > Desktop gains "Funny extras" (`\uf11d`) and "Hyprland quotes" (`\uf00a`) toggles; persisted via `quotesShowFun` / `quotesShowHypr` (both default true). Verified: surface renders in both toggle states, journal clean.
+ [X] Bar width: margin-mode gating (2026-09-11) -- `BarState.barWidth` now only applies in the margin modes (0 transparent, 1 solid margin, 3 glass margin) where it shrinks/centers the bar slab; full-bleed modes (2, 4, 5) ignore it and stay edge-to-edge. `bar/Bar.qml` added two lean `readonly` properties (`fullBleedMode`, `centeredMargin`) replacing the old duplicated floor-expressions; `margins.left/right` now read them in one shot. In Settings > Bar Size, the `IntStepRow` for width is dimmed + `enabled: false` in full modes, with a live hint: "Full-bleed mode — the bar spans the screen; width is ignored." Verified: width slider interactivity blanks in modes 2/4/5, centering works correctly in margin modes, clean journal.